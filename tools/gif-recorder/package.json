{
  "name": "amagra-gif-recorder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Records the three Line-1 demo GIFs against a local Amagra instance (issue #115). Isolated from the UI build.",
  "scripts": {
    "postinstall": "playwright install chromium",
    "record": "node record.mjs",
    "gif": "bash to-gif.sh"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}
